name: Deploy Backend to VM

on:
  workflow_run:
    workflows: ["Build and Push Docker Image to ACR"]
    types:
      - completed

env:
  ACR_NAME: ${{ secrets.ACR_NAME }}
  ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
  ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
  RG: ${{ secrets.RG_NAME }}
  BACKEND_VM: ${{ secrets.BACKEND_VM }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install Docker on VM
        run: |
          az vm run-command invoke \
           --command-id RunShellScript \
           --resource-group $RG \
           --name $BACKEND_VM \
           --scripts "
             sudo apt-get update -y

             sudo apt-get install -y \
             ca-certificates \
             curl \
             gnupg

             sudo install -m 0755 -d /etc/apt/keyrings

             curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
             sudo gpg --yes --dearmor -o /etc/apt/keyrings/docker.gpg

             echo \
              \"deb [arch=\$(dpkg --print-architecture) \
              signed-by=/etc/apt/keyrings/docker.gpg] \
              https://download.docker.com/linux/ubuntu \
              \$(lsb_release -cs) stable\" | \
              sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

             sudo apt-get update -y
             sudo apt-get install -y docker-ce docker-ce-cli containerd.io

             sudo usermod -aG docker azureuser
             sudo systemctl enable docker
             sudo systemctl start docker
             "

      - name: Deploy Backend to VM
        run: |
          az vm run-command invoke \
           --command-id RunShellScript \
           --resource-group $RG \
           --name $BACKEND_VM \
           --scripts "
           sudo docker stop backend || true
           sudo docker rm backend || true

            echo $ACR_PASSWORD | sudo docker login $ACR_NAME.azurecr.io \
               -u $ACR_USERNAME --password-stdin

           sudo docker pull $ACR_NAME.azurecr.io/backend:v1
           sudo docker run -d --name backend -p 8000:8000 $ACR_NAME.azurecr.io/backend:v1
           "

      # - name: Run IAST Agent Container
      #   run: |
      #     az vm run-command invoke \
      #       --command-id RunShellScript \
      #       --resource-group $RG \
      #       --name $BACKEND_VM \
      #       --scripts "
      #         echo $ACR_PASSWORD | docker login $ACR_NAME.azurecr.io -u $ACR_USERNAME --password-stdin
      #         docker stop backend-iast || true
      #         docker rm backend-iast || true
      #         docker pull $ACR_NAME.azurecr.io/backend-iast:v1
      #         docker run -d \
      #           --name backend-iast \
      #           -e CONTRAST_AGENT_KEY=${{ secrets.CONTRAST_AGENT_KEY }} \
      #           -e CONTRAST_AGENT_URL=${{ secrets.CONTRAST_AGENT_URL }} \
      #           --network host \
      #           $ACR_NAME.azurecr.io/backend-iast:v1
      #       "